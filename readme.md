# Virtuak-Key TCP/IP Server

Creates a TCP/IP Server which makes use of the Windows Hook [Hookname](Link) to detect KEYDOWN events and send the respective Virtual-Key Code to a Client using TCP/IP protocol.



